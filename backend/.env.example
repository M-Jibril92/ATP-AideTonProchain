# ===== VARIABLES D'ENVIRONNEMENT SÉCURISÉES =====
# IMPORTANT: Générez des valeurs fortes et uniques pour chaque déploiement!
# NE JAMAIS commiter ce fichier avec des vraies valeurs dans un repository public

# === Base de données ===
DATABASE_URL=sqlite:./bdd.sqlite
# Pour PostgreSQL: postgresql://user:password@localhost:5432/dbname
# Pour MySQL: mysql://user:password@localhost:3306/dbname

# === JWT (Authentification) ===
# Clés secrètes: Générez avec: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=votre_secret_jwt_tres_long_et_securise_minimum_32_caracteres
JWT_REFRESH_SECRET=votre_secret_refresh_token_tres_long_different_minimum_32_caracteres

# === Serveur ===
PORT=5000
NODE_ENV=development

# === CORS ===
FRONTEND_URL=http://localhost:5173

# === Stripe (pour les paiements) ===
STRIPE_PRIVATE_KEY=sk_test_votre_clé_privée
STRIPE_PUBLIC_KEY=pk_test_votre_clé_publique

# === Email (pour notifications) ===
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=votre_email@gmail.com
SMTP_PASSWORD=votre_mot_de_passe_app
